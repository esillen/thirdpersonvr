[project]
name = "thirdpersonview-prototype"
version = "0.1.0"
description = "Local prototype for marker-driven camera switching"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic",
  "numpy",
]
